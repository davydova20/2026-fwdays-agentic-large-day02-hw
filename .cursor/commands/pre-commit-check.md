# Pre-commit check

For the files I changed (or the current branch diff):

1. List which `.cursor/rules` apply and whether the change complies.
2. Suggest exact `yarn` commands from this repo (`test:code`, `test:typecheck`, relevant `vitest` scope).
3. Note anything that needs manual QA (canvas, export, Firebase).

Do not edit files unless I ask; focus on verification steps.
